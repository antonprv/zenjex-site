{
  "version": "v2.6.0",
  "date": "2026-01-20",
  "ru": [
    {
      "type": "new",
      "title": "ZenjexRunner — Pass 3",
      "items": [
        "Третий проход инъекции: `SceneManager.sceneLoaded` для аддитивно загруженных сцен.",
        "Объекты, инъецированные в этом проходе, уже выполнили `Awake()` — раннер выдаёт предупреждение `ZNX-LATE`.",
        "Используйте `ZenjexBehaviour` для гарантированной инъекции до `Awake()` в аддитивных сценах."
      ]
    },
    {
      "type": "new",
      "title": "ZenjexInjector — кэш рефлексии",
      "items": [
        "Добавлен `Dictionary<Type, TypeZenjexInfo>` с тремя массивами на тип: `FieldInfo[]`, `PropertyInfo[]`, `MethodInfo[]`.",
        "Кэш строится лениво и проходит вверх по цепочке наследования.",
        "`HasZenjexMembers(Type)` — быстрая предварительная проверка; пропускает `MonoBehaviour` без `[Zenjex]`-членов."
      ]
    },
    {
      "type": "new",
      "title": "BindingBuilder — BindInterfaces / BindInterfacesAndSelf",
      "items": [
        "`.BindInterfaces()` — регистрирует под всеми интерфейсами конкретного типа.",
        "`.BindInterfacesAndSelf()` — регистрирует под всеми интерфейсами и самим конкретным типом."
      ]
    },
    {
      "type": "fixed",
      "title": "Исправления",
      "items": [
        "ZenjexBehaviour: двойная инъекция при Reload Domain = false больше не происходит.",
        "ContainerBuilder: `RegisterFactory<T>` теперь корректно обрабатывает `Scoped + Lazy`."
      ]
    }
  ],
  "en": [
    {
      "type": "new",
      "title": "ZenjexRunner — Pass 3",
      "items": [
        "Third injection pass: `SceneManager.sceneLoaded` for additively loaded scenes.",
        "Objects injected here already had their `Awake()` run — the runner emits a `ZNX-LATE` warning.",
        "Use `ZenjexBehaviour` for guaranteed pre-Awake injection in additive scenes."
      ]
    },
    {
      "type": "new",
      "title": "ZenjexInjector — reflection cache",
      "items": [
        "Added `Dictionary<Type, TypeZenjexInfo>` with three arrays per type: `FieldInfo[]`, `PropertyInfo[]`, `MethodInfo[]`.",
        "Cache is built lazily and walks the full inheritance chain.",
        "`HasZenjexMembers(Type)` — fast pre-check; skips `MonoBehaviour` types with no `[Zenjex]` members."
      ]
    },
    {
      "type": "new",
      "title": "BindingBuilder — BindInterfaces / BindInterfacesAndSelf",
      "items": [
        "`.BindInterfaces()` — registers under all interfaces of the concrete type.",
        "`.BindInterfacesAndSelf()` — registers under all interfaces and the concrete type itself."
      ]
    },
    {
      "type": "fixed",
      "title": "Bug fixes",
      "items": [
        "ZenjexBehaviour: double injection with Reload Domain = false no longer occurs.",
        "ContainerBuilder: `RegisterFactory<T>` now correctly handles `Scoped + Lazy`."
      ]
    }
  ]
}
